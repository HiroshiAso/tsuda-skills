---
name: Googleカレンダー登録
description: hiroshinkg@gmail.comのGoogleカレンダーに予定を登録するスキル。開始時間指定時は1時間枠、終日指定時は終日予定とする。
---

# Googleカレンダー予定登録

ユーザーの指示に従い、`hiroshinkg@gmail.com` のGoogleカレンダーに予定を追加します。

## ブラウザ起動とアカウント確認

1. **プロファイルの特定**
   - `C:\Users\tsuda_hi\AppData\Local\Google\Chrome\User Data\Local State` を読み込み、`hiroshinkg@gmail.com` に対応するプロファイルディレクトリ名（例: `Profile 2`）を特定する。
     - JSON内の `profile.info_cache` セクションを確認し、`user_name` が `hiroshinkg@gmail.com` となっているエントリを探す。

2. **ブラウザの起動**
   - 特定したプロファイルディレクトリを指定して Chrome を起動する。
   - PowerShell コマンド例:
     ```powershell
     Start-Process chrome -ArgumentList '--profile-directory="Profile 2"', 'https://calendar.google.com/'
     ```
     （※ `"Profile 2"` の部分は実際に特定したプロファイル名に変更する）

3. **アカウント確認**
   - 起動したブラウザで、ログイン中のアカウントが `hiroshinkg@gmail.com` であることを確認する。

## 予定の作成手順

1. **予定作成の開始**
   - 左上の「作成」ボタン(または＋ボタン)をクリックし、「予定」を選択する。
   - またはショートカットキー `c` を使用する。

2. **日時の設定**
   - ユーザーの指示（プロンプト）に基づき、以下のルールを適用する：
     - **開始時間が指定された場合**
       - 開始日時：ユーザーが指定した時間
       - 終了日時：開始時間の **1時間後** (1時間の枠を確保する)
     - **「終日」と指定された場合**
       - 「終日」のチェックボックスをオンにする。
     - **時限（1限、2限など）が指定された場合**
       - **1限**: 09:30 開始
       - **2限**: 11:15 開始
       - **3限**: 13:30 開始
       - **4限**: 15:15 開始

3. **その他の設定**
   - **タイトル**: ユーザーが指定した件名を入力する。件名が明示されていない場合は「予定」とするか、文脈から推測される内容を入力する。

4. **保存**
   - 「保存」ボタンをクリックして登録を完了する。
